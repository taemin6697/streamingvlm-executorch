# Unified VLM Foundation

Project-local foundation layer for mobile VLM execution without patching ExecuTorch source.

Use the superbuild script to build the upstream ExecuTorch backend tree and the foundation
runner in one command:
